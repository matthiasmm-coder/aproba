import { EnlaceNoDisponible } from "@/components/enlace-no-disponible";

// Token de portal que no resuelve → pantalla del cliente final (no el 404 del gestor).
export default function NotFound() {
  return <EnlaceNoDisponible />;
}
